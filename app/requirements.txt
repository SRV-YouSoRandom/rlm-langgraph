# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12

# LangChain ecosystem
# Pin major versions only — let pip resolve compatible sub-dependencies.
# DO NOT hard-pin patch versions across this ecosystem; they have cascading
# interdependencies (pydantic-settings, langchain-core, etc.) that pip must solve.
langchain>=1.2.0,<2.0.0
langchain-community>=0.4.0,<1.0.0
langchain-openai>=0.3.0,<1.0.0
# langchain 1.x moved text splitters to their own package
langchain-text-splitters>=0.3.0,<1.0.0

# LangGraph v1.x
# Pin major version only for same reason.
langgraph>=1.0.0,<2.0.0
langgraph-checkpoint>=2.0.0,<3.0.0
langgraph-checkpoint-sqlite>=2.0.0,<3.0.0
langgraph-prebuilt>=1.0.0,<2.0.0

# Embeddings
sentence-transformers>=3.3.0,<4.0.0

# Vector Store
qdrant-client>=1.12.0,<2.0.0

# PDF Parsing
pymupdf>=1.24.0,<2.0.0

# Config & Validation — let pip pick compatible version
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0

# Utils
python-dotenv>=1.0.0
httpx>=0.27.0,<1.0.0

# RLM — Sandboxed REPL
RestrictedPython>=7.0.0

# Async SQLite
aiosqlite>=0.20.0

# Observability (Optional)
# langsmith