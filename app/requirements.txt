# API
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# LangChain (v1.x stable)
langchain==1.2.0
langchain-community==0.4.1
langchain-openai==0.3.0

# LangGraph v1.x (NEW)
# Core graph framework
langgraph==1.0.7
# Base checkpoint interface (includes MemorySaver)
langgraph-checkpoint==2.0.10
# SQLite checkpointer — now a separate package since v0.2
langgraph-checkpoint-sqlite==2.0.5
# Prebuilt helpers (ToolNode, create_react_agent etc.)
langgraph-prebuilt==1.0.8

# Embeddings
sentence-transformers==3.3.1

# Vector Store
qdrant-client==1.12.0

# PDF Parsing
pymupdf==1.24.13

# Config & Validation
pydantic==2.10.0
pydantic-settings==2.6.1

# Utils
python-dotenv==1.0.1
httpx==0.27.2

# RLM — Sandboxed REPL
RestrictedPython==7.4

# Async SQLite (needed by AsyncSqliteSaver)
aiosqlite==0.20.0

# Observability (Optional)
# langsmith